# app/api/__init__.py
# This file makes the 'api' directory a Python package.

# It could optionally define a main API blueprint if all API routes were simple
# or used to register nested blueprints, but currently, each file defines its own blueprint.