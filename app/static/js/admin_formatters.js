// app/static/js/admin_formatters.js
// This file contains client-side formatting logic for the admin panel.

document.addEventListener('DOMContentLoaded', function() {
    // All number formatting is now handled by the backend via Babel.
    // This file is kept for potential future admin-specific JS, but the number formatting logic is no longer needed.
    console.log("[AdminFormatters] Script loaded. Number formatting is now handled server-side.");
});